build.env=local

env {
  stage {
    classifier.host="http://ip-172-23-7-212.ec2.internal:8003"
    ramp.user.num=100
    requests.per.user=20
  }
  local {
    host.url="http://localhost:8000"
  }
  prod {
    classifier.host="http://internal-classifier-785588950.us-east-1.elb.amazonaws.com"
    ramp.user.num=100
    requests.per.user=20
  }
}